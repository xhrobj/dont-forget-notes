# Кодирование

## Base64

**Base64** --- это способ представить любые двоичные данные (файлы,
байты) в виде текста, состоящего только из латинских букв, цифр, `+`,
`/` и `=`.

Это полезно, например, когда нужно безопасно передавать данные через
текстовые каналы (как email или JSON), которые не дружат с «сырыми»
байтами.

Работает так: берутся каждые **3 байта (24 бита)**, разбиваются на **4
блока по 6 бит**, и каждый из них кодируется одним символом из таблицы
Base64.\
Если данных не хватает до трёх байт --- добавляются специальные символы
`=` (это называется **паддинг**).

В принципе, для передачи через текстовые каналы можно было бы
закодировать двоичные данные в **hex**, но hex будет длиннее:\
- в Base64 **3 байта = 4 символа**\
- в hex **3 байта = 6 символов**

------------------------------------------------------------------------

### Пример для строки `"Hello, world!"`

🔢 **Hex**:

    48656c6c6f2c20776f726c6421

*(24 символа)*

🔡 **Base64**:

    SGVsbG8sIHdvcmxkIQ==

*(20 символов, включая паддинг `==`)*

------------------------------------------------------------------------

## Base32

🔡 **Base32** --- это похожее кодирование, но каждый символ кодирует **5
бит** (а не 6, как в Base64).\
Алфавит: `A–Z` и `2–7`, всего 32 символа.

Принцип работы:\
1. Берём двоичные данные и объединяем в одну длинную битовую строку.\
2. Делим её на блоки по 5 бит.\
3. Каждый блок превращается в число от 0 до 31.\
4. По таблице выбираем символ (0 → `A`, 1 → `B`, ..., 31 → `7`).\
5. При нехватке бит добавляем паддинг `=` до кратности 8 символам.

Особенности:\
- Строка длиннее, чем в Base64 (накладка \~60% против \~33%), **но короче чем hex** (где 1 байт представлен двумя символами, что дает +100%).\
- Но безопаснее для систем, где недопустимы спецсимволы (`+`, `/`) или
разные регистры.\
- Поэтому Base32 часто применяют в Tor-адресах и DNS.

Пример:

    "M" → "JU======"

---

Итог по компактности:

То есть если было 100 байт:

hex → 200 символов

base32 → 160 символов

base64 → 133 символа

------------------------------------------------------------------------

## URL encoding (percent-encoding)

**URL encoding** --- это способ записывать в адресной строке браузера
символы, которые там обычно недопустимы (например, пробелы, кириллицу,
спецсимволы). Принцип простой: символ заменяется на `%` и его **hex-код
в ASCII/UTF-8**. Например, пробел превращается в `%20`, а символ `?` ---
в `%3F`. Это позволяет передавать любой текст в URL без ошибок и
неоднозначностей.

------------------------------------------------------------------------

## Punycode (IDN encoding)

**Punycode** --- это стандарт, который позволяет записывать **доменные
имена с национальными символами** (например, кириллица, китайский,
арабский) в виде допустимых для DNS ASCII-символов. Работает так: домен
переводится в строку, начинающуюся с `xn--`, а затем идёт закодированная
часть. Например:

    пример.рф → xn--e1afmkfd.xn--p1ai

Таким образом, DNS работает только с латиницей, но пользователи могут
вводить домены на своём языке.

---

🎯 Сравнение

Base64/Base32/Hex — про представление любой бинарщины в печатных символах. Про «байты → безопасный текстовый алфавит» (обычно для хранения или передачи данных).

URL encoding — для безопасной передачи текста внутри URL. Про то, как безопасно записать уже существующие байты в URL-строке.

Punycode — мост между Unicode-доменами и ASCII-ограниченным DNS.