# 🌐 Эволюция архитектуры API и «большая четвёрка»

## 📊 Исторический контекст

Чтобы понять, как появились современные подходы к построению API, важно видеть их развитие во времени:

| Этап | Главный протокол |
|------|------------------|
| ~2000–2010 | 🏆 **SOAP** — основной стандарт корпоративной интеграции |
| ~2010–2020 | 🚀 **REST** — «новый стандарт» для веб-API |
| ~2015–2025 | ⚙️ **gRPC**, **GraphQL** — специализированные и гибкие альтернативы |

SOAP заложил основы обмена данными между системами. REST сделал веб‑API простыми и повсеместными. gRPC и GraphQL появились как реакция на ограничения REST — первый оптимизирован под производительность и строгую типизацию, второй дал клиентам контроль над структурой данных.

---

## 📡 Большая четвёрка архитектурных подходов к API

**Большая четвёрка API** — это четыре ключевых архитектурных подхода к построению прикладных интерфейсов поверх HTTP.  
Они определили развитие веб‑сервисов и до сих пор остаются базовыми моделями взаимодействия клиент–сервер.

| Подход | Что это | Краткое описание | Где используется |
|--------|--------|------------------|------------------|
| **SOAP** | Протокол | Строгий XML‑контракт (WSDL), операции, WS‑Security, RPC‑модель. Старейший и всё ещё живой в корпоративной и гос‑инфраструктуре. | Банки, госуслуги, корпоративные интеграции |
| **REST** | Архитектурный стиль | Ресурсы через URL, методы HTTP = операции. Прост, гибок, повсюду. Основной выбор для публичных API. | SaaS, веб‑приложения, мобильные API |
| **gRPC** | RPC‑фреймворк | Компактный бинарный формат (Protobuf), автогенерация клиента и сервера, поверх HTTP/2. | Микросервисы, backend‑to‑backend коммуникация |
| **GraphQL** | Язык запросов | Клиент сам описывает нужные данные, один эндпойнт, высокая гибкость. | Frontend‑ориентированные API, BFF‑слой, агрегаторы |

Хотя эти четыре подхода считаются основными, существуют и другие варианты: JSON‑RPC, OData, Falcor, tRPC и др. Но именно **SOAP, REST, gRPC и GraphQL** представляют четыре базовые парадигмы.

---

## 🧱 Слои API (от верхнего к нижнему)

API‑взаимодействие можно представить как набор слоёв — от структуры данных до транспортного уровня:

| Уровень | Примеры | Что определяет |
|--------|----------|------------------|
| 📄 **Формат данных** | JSON, XML, Protobuf, SDL | Структура передаваемой информации |
| 🔌 **Способ организации API** | REST, SOAP, gRPC, GraphQL | Как организовано взаимодействие |
| 🌍 **Протокол доставки (L7)** | HTTP/1.1, HTTP/2, HTTP/3 | Как происходит обмен сообщениями |
| 📶 **Транспорт (L4)** | TCP, QUIC | Как данные доставляются по сети |

📌 Эти уровни строятся друг на друге: формат данных «живёт» внутри архитектурного стиля, тот использует конкретный протокол доставки, который, в свою очередь, передаётся по транспортному соединению.

---

## 📚 Итог

- SOAP, REST, gRPC и GraphQL — это не конкурирующие форматы, а четыре архитектурные модели поверх HTTP.  
- Они появились в разное время и для разных задач, но сегодня сосуществуют в одной экосистеме.  
- Понимание их исторической эволюции и места в сетевой модели помогает осознанно выбирать инструменты для проектов.
