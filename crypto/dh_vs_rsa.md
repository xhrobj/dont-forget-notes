
# Краткое напоминание: DH vs DHE vs RSA

## ✅ DH (Диффи–Хеллман)

**Что делает:**  
Позволяет **двум сторонам безопасно выработать общий симметричный ключ**, даже если обмен идёт по открытому каналу.

**Тип:**  
- Алгоритм **обмена ключами**
- Не шифрует сообщения напрямую
- Основан на **дискретном логарифме**

**Применение:**  
- Старые версии TLS (не используется в TLS 1.3)
- Криптографические протоколы 1990-х годов

---

## ✅ DHE (Diffie–Hellman Ephemeral)

**Что делает:**  
Вариант DH, где используются **временные (одноразовые) ключи** для каждого соединения.

**Преимущества:**  
- Обеспечивает **Forward Secrecy** (совершенная прямая секретность) — даже если закрытый ключ (имеется в виду секретное число и dh-пары чисел) сервера скомпрометирован, прошлые сессии останутся в безопасности. Именно это и означает совершенную прямую секретность - под угрозой будет только текущая сессия, а все прошлые в безопасности (в случае, если кто-то перехватил их и сохранил) - от них приватных dh-чисел уже не существует - они удаляются после завершения каждой сессии.

**Тип:**  
- Обмен ключами (DH)
- Сессионные ключи (ephemeral = одноразовые)

**Применение:**  
- TLS 1.2 (DHE и ECDHE)
- SSH
- Некоторые VPN

## ⚠️ Отличие DH от DHE ##

Технически, DH и DHE — это один и тот же алгоритм.

**💣 Почему DH без Ephemeral уязвим?**

- Сервер хранит одну и ту же DH-пару (открытый + закрытый)

- При соединении клиент получает g^a, сервер отвечает g^b, и обе стороны вычисляют g^ab

- Но если кто-то в будущем украдёт b — он сможет расшифровать все прошедшие сессии, если у него есть записи трафика

**🧠 Почему DHE защищён от этого?**

- При каждом соединении сервер и клиент генерируют новые временные DH-пары (g^x, g^y)

- После завершения сессии — эти ключи стираются

- Даже если злоумышленник запишет трафик, а потом украдёт ключи сервера — он ничего не сможет расшифровать, потому что временные DH-ключи больше недоступны

---

## ✅ RSA

**Что делает:**  
Используется для **асимметричного шифрования и цифровой подписи**.

- **Зашифровываем** открытым ключом, **расшифровываем** закрытым
- Или: **подписываем** закрытым, **проверяем** открытым

**Тип:**  
- Асимметричный алгоритм (открытый + закрытый ключ)
- Основан на **факторизации больших чисел**

**Применение:**  
- TLS (в старых версиях — для обмена ключом)
- Подписи кода, сертификаты
- PGP / S/MIME
- SSH

---

## 📦 Сравнение DH / DHE / RSA

| Алгоритм | Задача                          | Тип               | Forward Secrecy |
|----------|----------------------------------|-------------------|-----------------|
| DH       | Выработка общего секрета        | Обмен ключом      | ❌ Нет           |
| DHE      | То же, но с одноразовыми ключами| Обмен ключом      | ✅ Да            |
| RSA      | Шифрование или подпись          | Асимметрия        | ❌ Нет           |

---

## 🤝 Как они работают вместе в TLS (гибридное шифрование)

1. Сервер подписывает параметры DHE с помощью **RSA** или **ECDSA**
2. Клиент проверяет подпись — удостоверяется, что это тот самый сервер
3. Стороны проводят **(EC)DHE** и получают **общий симметричный ключ**
4. Все данные шифруются **AES** или **ChaCha20**
