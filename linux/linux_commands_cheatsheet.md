# 📝 Шпаргалка по командам Linux

Оболочки (Shell - программа, котораая принимает команды пользователя и выполняет их через взаимодействие с ОС): bash, zsh (баш и зи-эш)

---

## 🗃 Работа с директориями

Перемещение по каталогам, просмотр файлов и директорий в них, а также:
создание, удаление, копирование, переименование, перемещение.

В Windows эти задачи решает File Explorer, в macOS - Finder. В консоли Linux этим занимаются отдельные утилиты.

- `ls` — отобразить (list) содержимое каталога

- `cd` — перемещение по каталогам  
  Примеры: `cd -` (возврат к предыдущему каталогу), `cd ~` (переход к домашней директории пользователя)

- `pwd` — абсолютный путь к текущей директории

- `mkdir` — создает директорию
  Примеры: 
    - `mkdir folder` - если каталог существует, будет ошибка
    - `mkdir -p folder` - c опцией -p (--parent) можно создавать вложенные каталоги (при этом, если любой из каталогов существует, ошибки не будет)

- `mv SRC... DST` — переместить/переименовать каталог
  в отличие от mkdir, у этой команды нет опции -r (она по дефолту умеет работать с вложенными директориями)
  подробнее см. в разделе "Работа с файлами"

- `cp -r SRC... DST` — копировать директорию
  Примеры:
    - `cp --recursive folder forder1`
    - `cp --recursive folder forder1 folder2` - (folder2 должен существовать)

- `rm -r` — удалить директорию
  Пример: `rm --recursive folder`

---

## 📁 Работа с файлами

- `touch` — создает файл
  Пример: `touch readme.MD`

- `cp SRC... DST` — копировать файл
  Примеры: 
    - `cp readme.MD license.txt`
    - `cp -i readme.MD license.txt` - спросить перед перезаписью (--interactive), если такой файл уже существует
    - `cp readme.MD license.txt folder/` - скопировать несколько файлов в каталог

- `mv SRC... DST` — переместить/переименовать файл/каталог
  в SRC могут быть одновременно и файлы и папки (в DST в этом случае только папка, которая должна существовать)
  Примеры: 
    - `mv readme.MD license.txt`
    - `mv -i readme.MD license.txt` - спросить перед перезаписью (--interactive), если такой файл уже существует
    - `mv readme.MD license.txt folder/` - скопировать несколько файлов в каталог

- `rm` — удалить файл
  Пример: `rm readme.MD`

---

## 🔤 Работа с текстом

- `echo` — выводит текст в консоль.  
  Пример: `echo "Привет, Linux!"`

- `wc` — выводит количество строк, слов и символов в файле
  Пример: `wc animals.txt`

- `cat` — выводит содержимое файла в консоль.  
  Пример: `cat /etc/os-release`

- `less` — открывает содержимое файла в консоли в постраничном интерактивном режиме
  q - выход, space - следующая страница, b - предыдущая страница
  Пример: `less /etc/os-release`

---

## 👤 Работа с пользователями и правами

- `whoami` — выводит имя текущего пользователя (или root для суперпользователя)

---



## 🔧 Системная информация

- `uname` — версия ядра
- `date` — системная дата

---

## 📦 Управление пакетами

_(пока пусто, добавлю позже)_

---

## 🌐 Сетевые команды

_(пока пусто, добавлю позже)_

---

## 🛠 Управление процессами

_(пока пусто, добавлю позже)_

---

## 📜 Разное

_(пока пусто, добавлю позже)_