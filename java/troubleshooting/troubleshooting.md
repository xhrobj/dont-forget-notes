# Шпаргалка по Java Troubleshooting (ИПР)

Возможности для анализа проблем при авариях/ситуациях с приложением на Java

---








## 1) Stacktrace

### Зачем нужен stacktrace
Stacktrace показывает **где именно произошла ошибка** и **какая цепочка причин к ней привела**.

Это самый быстрый способ понять:
- что сломалось,
- в каком месте,
- из-за чего (Caused by),
- какой код вызвал проблему.

### Как читать stacktrace

- Ищи нижний **Caused by** - это **root cause** — первопричина, из-за которой всё упало; выше — последствия/обёртки.
- Смотри на первые 3–5 кадров **твоего** кода (ищи пакеты: com.mycompany, ru.xxx.project,..) - это самая полезная точка входа.
- Что означает `Suppressed`— «это не главный эксепшн, а дополнительный, который произошёл во время обработки другого исключения и был прицеплен к нему». В Java это чаще всего появляется при try-with-resources (например: Suppressed: java.io.IOException: stream closed)

### Итого: 🧠 алгоритм анализа

1. Найди нижний **Caused by**.  
2. Поднимись вверх до первого фрейма твоего кода.  
3. Определи контекст (null, сеть, БД, сериализация).  
4. Проверь аргументы и значения, которые могли привести к проблеме.

### Инструменты

***IntelliJ IDEA → Analyze Stack Trace…***
- Вставляешь stacktrace → IDE раскрывает нужные файлы.  
- Позволяет быстро прыгать к строкам кода.
- Удобно для длинных и вложенных стеков.

### Дополнительно

- [Какие данные есть в исключении?](java-throwable-cheatsheet.md)
- [Примеры Exception'ов](stacktrace_examples.md)

### 🏷️ Полезные JVM-флаги

Если стектрейса нет, исправить можно так:

`-XX:-OmitStackTraceInFastThrow` выключает оптимизацию HotSpot, при которой у часто бросаемых исключений (например, NullPointerException, ArrayIndexOutOfBoundsException, ArithmeticException, ClassCastException и др.) перестают заполнять стек после «прогрева» кода.

Что это даёт: по умолчанию (OmitStackTraceInFastThrow = true) при многократных NPE/AIOOBE и т.п. JVM начинает кидать «быстрые» экземпляры без stacktrace (экономия времени на fillInStackTrace()).

С флагом `-XX:-OmitStackTraceInFastThrow` стек будет всегда заполняться, и ты увидишь полноценный stacktrace там, где раньше он пропадал.

### Что еще нужно помнить

- Размер стека регулируется ключом: `-Xss` (размер стека в 64-битной архитектуре по дефолту 1мб)
- Стек — персональный для потока.
- Много потоков × большой `-Xss` = заметная память «только на стеки».
- Heap/Metaspace/CodeCache — общие для всех потоков процесса JVM.

### ✔️ Короткое резюме

- ***Stacktrace — самый важный инструмент диагностики.***
- Ищи Caused by → затем свои методы → затем контекст → затем параметры.
- Если stacktrace исчезает — используй -XX:-OmitStackTraceInFastThrow.
- Для анализа всегда удобно использовать IDEA → Analyze Stack Trace…

---








## 2) Thread dump

### Что это (по‑простому)
**Thread dump** — моментальный снимок **всех потоков JVM**: имя, состояние (`RUNNABLE / BLOCKED / WAITING`), стек вызовов и удерживаемые / ожидаемые замки (мониторы).

Он показывает что делал каждый поток в момент времени.

### Зачем нужен Thread dump (в каких случаях его нужно снимать)
- приложение «подвисает», таймауты, очередь запросов;
- высокий CPU — найти «горячий» поток и место в коде;
- проблемы с синхронизацией, блокировки, дедлоки (JVM покажет блок **Found one Java-level deadlock**);
- медленная БД/сеть (видно по WAITING на IO).

### Как выглядит (очень кратко)
```vb
"worker-7" nid=0x1a RUNNABLE
  at com.acme.Service.loop(Service.java:42)
"pool-1-thread-3" WAITING
  at java.util.concurrent.FutureTask.get(...)
"cache-writer" BLOCKED
  - waiting to lock <0xABCD> held by "cache-reader"
```

### Какие состояния бывают

- ***RUNNABLE*** — (CPU/IO) поток либо на CPU, либо в системном вызове (не означает автоматически «жрёт CPU»)
- ***BLOCKED*** — ждёт монитор (synchronized) → признак контеншна.
- ***WAITING / TIMED_WAITING*** — ожидает событие: park, wait, Future.get, I/O - нормальное состояние для idle-потоков.

### Сколько дампов снимать
Сделай **3–5 дампов** с интервалом **5–10 с**, чтобы увидеть динамику (движется ли стек). Если стек не меняется → поток завис.

### Как снять 

***Рекомендуемый способ***

```bash
jcmd <pid> Thread.print > threaddump.txt
```

***Альтернативный вариант***

```bash
jstack -l <pid> > threaddump.txt
```

***В stdout приложения (полезно в Docker/Kubernetes)***

```bash
kill -3 <pid>
```

Dump появится в логах приложения.

***Из Java-кода (ограниченно полезно)***

```java
Thread.getAllStackTraces()
```

Не поможет, если JVM зависла.

### Как читать Thread Dump

***1) Найди RUNNABLE-потоки***

Особенно с длинным стеком — возможные CPU hotspots.

***2) Найди BLOCKED***

Ищи паттерны:
- waiting to lock <0x...>
- locked <0x...>
- held by <thread>

***3) Сопоставь поток по CPU***

linux:

```bash
top -H -p <pid>
```

Найди nid → затем найди его в дампе (nid=0xNNN).

***4) Ищи дедлоки***

Если есть дедлок, JVM сама напишет:

```
Found one Java-level deadlock:
```

### Визуализация

- IntelliJ IDEA → Code → Analyze Thread Dump…
- https://jstack.review - удобная визуализация мониторов и группировка потоков

### Связанные материалы

- [Контеншн и Дедлок](contention-vs-deadlock.md)

### Дополнительно -  высокий CPU в Java бывает из-за:

- Бесконечных циклов / busy-wait.
- Тяжёлых бизнес-алгоритмов.
- Контеншна (ждут лок), но один поток — на CPU.
- Сборщика мусора (GC thrashing).
- Нативных вызовов / I/O в native.
- JIT, Code Cache, агенты, профайлеры.

### ✔️ Короткое резюме

Thread dump — ключевой инструмент при зависаниях, таймаутах и высоком CPU.
Используй jcmd Thread.print, снимай несколько дампов подряд и сопоставляй nid с CPU.

---








## 3) Heap dump

### Что это (по-простому)
**Heap dump** — «фотография» **кучи Java** в один момент времени:  
объекты, поля/ссылки, массивы, классы/класс-лоадеры и **GC-корни**.

👉 Важно: heap dump **не содержит** стеки потоков и не показывает «кто создал объект».

### Когда нужен
- `OutOfMemoryError` (особенно `Java heap space`);
- по метрикам видно **рост heap** / подозрение на утечку памяти;
- нужно понять, какие типы объектов занимают больше всего памяти.

### Как снять

***Автоматически при OOME***

```bash
-XX:+HeapDumpOnOutOfMemoryError
-XX:HeapDumpPath=/dumps   # каталог или полный путь к файлу .hprof

# можно дампить до полной или после полной сборки мусора
# что это используется в продвинутом анализе, но реже.
-XX:+HeapDumpBeforeFullGC
-XX:+HeapDumpAfterFullGC
```

***Вручную (онлайн-приложение)***

```
# :live в jmap вызывает Full GC перед снятием дампа → это может привести к длинной паузе
jmap -dump:live,format=b,file=heap.hprof <pid>

# или
# вот так пробовал в нашем неймспейсе
# MAT не использует расширение файла, формат определяется заголовком. Поэтому можно не переименовывать в .hprof
jmap -dump:format=b,file=/tmp/heapdump.bin <pid>

# или
jcmd <pid> GC.heap_dump /path/heap.hprof
```

- В Kubernetes: `kubectl exec -it <pod> -- jcmd 1 GC.heap_dump /dumps/heap.hprof`

Файл может весить ~объём используемой кучи. Убедись, что путь существует и доступен на запись (в контейнере — смонтировать volume).

### Чем открывать
- VisualVM — удобно для простых кейсов.
- Eclipse MAT (Memory Analyzer Tool) — мощный анализатор утечек, Leak Suspects.
- JDK Mission Control / JFR — не открывает .hprof, но даёт allocation / old object профили (кто создаёт объекты).

### Что смотреть в инструменте
- **Dominator Tree** + **Retained Size** → «киты» — объекты/коллекции, которые удерживают больше всего памяти.
- **GC Roots / Nearest GC Root** — путь удержания (часто: `static` коллекции, кэши, `ThreadLocal`, класс‑лоадеры).
- **Class Histogram** → подозрительные типы и их количество (например, много byte[], String, DTO).
- В MAT: отчёт Leak Suspects.

### Быстрый алгоритм чтения
1. Нажми **Compute Retained Sizes**.
2. Отсортируй классы по **Retained Size** — найди «китов».
3. Открой «кита» в **Dominator Tree**, посмотри путь до GC‑root.
4. Соотнеси с кодом: кто туда кладёт и почему не чистит.
5. Исправь: лимиты кэша, очистку коллекций, жизненный цикл объектов.

### Частые ловушки
- `:live` в `jmap` может «заморозить» на дольше — используем аккуратно.
- Большие массивы/буферы (например, `byte[]`) могут «маскировать» владельца; смотри, кто на них ссылается.
- Heap dump **не показывает** «кто создал» — для этого используется JFR / allocation profiling.
- Размер файла может быть десятки/сотни МБ — нужно место на диске и время на загрузку в инструмент.

### ✔️ Короткое резюме

Heap dump — основной инструмент при OutOfMemoryError и утечках памяти.
Снимаем его через `-XX:+HeapDumpOnOutOfMemoryError` или `jcmd GC.heap_dump`,
открываем в VisualVM / Eclipse MAT и смотрим dominator tree + retained size,
чтобы найти объекты, которые удерживают кучу и понять, кто их не отпускает.

---







## 4) Core dump (native crash) - «снимок при смерти процесса»

### Что это (по-простому)

**Core dump** — это «фотография» **всего процесса** на уровне операционной системы.  

Если heap dump показывает только объекты Java, то core dump — всю память процесса (всё, что было в RAM):
- Java-heap,
- Metaspace,
- native-heap,
- стеки потоков (native + Java),
- регистры процессора,
- загруженные библиотеки (`.so` / `.dylib` / `.dll`),
- служебные структуры ОС.

👉 Core dump создаёт **операционная система**, а не сама JVM.  
Обычно — при аварийном завершении процесса (`SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGABRT` и т.п.).

Стандартными JVM-инструментами (`jcmd`, `jmap`) *core вызвать нельзя* —  
они работают только с живым процессом.

Из неочевидного (из Паньгина): если процесс уже умер,  
heap dump с него не снять, но можно взять core-файл и «натравить» на него `jmap`/`jstack` через `jhsdb`.

### Когда нужен

- JVM упала с `SIGSEGV` / `SIGBUS` / `Internal Error` (native crash);
- подозрения на баг в **JNI/Unsafe** или нативной библиотеке (OpenSSL, драйвер, С-код и т.д.);
- расследование редких крашей в продакшене;
- анализ низкоуровневых проблем с памятью.

### Что в него попадает

- Вся адресное пространство процесса: heap, metaspace, native-память, стеки потоков;
- состояние регистров CPU для каждого потока;
- список/карта загруженных нативных библиотек;
- контекст: PID, UID, сигнал, время, версия ядра/ОС.

### Как появляется core dump

Решение принимает **ОС** (лимиты и настройки ядра).  

JVM со своей стороны может:

```bash
-XX:+CreateCoredumpOnCrash      # разрешить писать core при падении JVM
-XX:ErrorFile=/logs/hs_err_pid%p.log
```

В итоге при native-краше обычно получаем два файла:

- `hs_err_pidXXXX.log` — короткий отчёт JVM (указание, где упало);
- `core` — большой бинарный файл (вся память процесса).

### Дополнительно

```bash
-XX:+CrashOnOutOfMemoryError
```

превращает OutOfMemoryError в фатальный краш → тогда тоже появятся hs_err + core (если разрешён).

### ✔️ Короткое резюме
Core dump — это «черный ящик» процесса: полный снимок памяти и состояния на момент краша.

Используется реже, чем thread/heap dump, но может рассказать всё, что происходило в момент краша —  
на уровне как JVM, так и самой ОС.

---










## 5) Code Cache в JVM

### Что это (по-простому)

Когда Java-приложение работает, методы сначала выполняются **интерпретатором**.  
Если метод часто вызывается (становится «горячим»), JVM компилирует его через **JIT** в машинный код.

Этот скомпилированный код JVM помещает в специальную область нативной памяти:

> 💡 **Code Cache** — это участок *нативной памяти* (вне Java Heap), где JVM хранит скомпилированные JIT-методы.

### Как работает JIT и Code Cache

1. Метод запускается в интерпретаторе.  
2. JVM считает его «горячим» (по счётчикам профилирования).  
3. JIT-компилятор (**C1/C2**, в зависимости от Tiered Compilation) генерирует машинный код.  
4. Код размещается в **Code Cache**.  
5. Все следующие вызовы выполняются сразу как нативный код → быстрее.

📌 **Code Cache = хранилище JIT-кода.**
### Что происходит при переполнении

Если область Code Cache заполнена, JVM пишет в лог:

```
CodeCache is full. Compiler has been disabled.
```

Это значит:
- JIT больше не может размещать новые методы;
- новые методы будут исполняться интерпретатором;
- производительность падает (всё работает, но медленнее).

> ⚠️ Приложение не упадёт, но станет ощутимо медленнее при нагрузке.

---

### Как диагностировать

#### 1️⃣ Логирование
```bash
# JDK 9+
-Xlog:codecache=debug

# JDK 8
-XX:+PrintCodeCache
```
Будет видно размер, использование и процент заполнения.

#### 2️⃣ JMX-метрика
```
CodeCacheUsage
```
(можно смотреть в VisualVM, JConsole, Prometheus exporter).

#### 3️⃣ Команда jcmd
```bash
jcmd <pid> Compiler.codecache
```

Пример вывода:
```
CodeCache: size=240000Kb used=239000Kb max_used=239000Kb free=1000Kb
Compiler has been disabled.
```

### Как увеличить размер

Обычно Code Cache ≈ 240–480 МБ.

```bash
-XX:ReservedCodeCacheSize=512m
```
(или больше, если много динамически генерируемых классов/методов).

Для новых JVM (JDK 9+) можно регулировать подтипы:
```bash
-XX:ProfiledCodeHeapSize=256m
-XX:NonProfiledCodeHeapSize=128m
-XX:NonNMethodCodeHeapSize=32m
```

### Где это может случиться

- Spring / Hibernate — создают кучу CGLIB-прокси;  
- Kotlin / Scala / Groovy — активно используют лямбды и метапрограммирование;  
- Framework’и, которые генерируют код на лету (JRuby, Jython, ASM, ByteBuddy);  
- Долгоживущие системы с миллионами методов и загрузками классов.

---

### Что смотреть в логах

Типичные сигналы:
```
Compiler queue is full
CodeCache (profiled): size=256M used=256M max_used=256M free=0M
```
Если `free=0M` и `Compiler has been disabled` — кэш забит.

---

### Что делать при переполнении

1. Увеличить `-XX:ReservedCodeCacheSize` (до 512m, 1g — по ситуации).  
2. Проверить, нет ли чрезмерной генерации прокси/классов.  
3. Перейти на новую JDK — улучшенный JIT и управление кэшем.  
4. Мониторить `CodeCacheUsage` через JMX или логи.  
5. При необходимости — отключить tiered compilation:  
   ```bash
   -XX:-TieredCompilation
   ```
   (только для диагностики, не для продакшена).

---

### Сравнение с другими областями памяти

| Область | Назначение | Где находится | Что хранит |
|----------|-------------|----------------|-------------|
| **Heap** | Java-объекты | Managed (управляется GC) | Объекты, массивы |
| **Metaspace** | Метаданные классов | Native | Классы, методы, Constant Pool |
| **Code Cache** | Скомпилированный JIT-код | Native | Машинный код методов |


### ✔️ Короткое резюме

**Code Cache** — это участок нативной памяти, где JVM хранит скомпилированный JIT-код.  
Если он переполнится, JIT остановится, и приложение начнёт «тормозить». 

Решение: мониторинг, увеличение размера и контроль генерации новых методов.

---









## 6) Profiling в JVM

Практичный гид: зачем, чем и как профилировать Java‑приложение. Сфокусирован на продакшн‑безопасных способах (async-profiler, JFR) и быстрых шагах.

---

## 🧠 Что такое профилирование
**Профилирование** — это наблюдение за живым приложением во времени, чтобы понять:
- где сгорает **CPU** (горячие методы/стэки),
- где тратится **реальное время** (ожидания: блокировки, IO, syscalls),
- кто делает **наибольшее число аллокаций** (мусор и давление на GC),
- где есть **lock contention** (пробки на замках).

> Если thread dump — «фото», то профилирование — «видеозапись» поведения кода.

---

## 🧰 Инструменты и когда что использовать

| Инструмент | Что даёт | Нагрузка | Где запускать | Комментарий |
|---|---|---|---|---|
| **async-profiler** | CPU / Wall / Alloc / Lock, Flame Graph | низкая (<2%) | прод/стейдж/локал | самый точный и лёгкий; CLI |
| **JFR (Java Flight Recorder)** | CPU, GC, IO, аллокации, события JVM | низкая (1–2%) | прод/стейдж/локал | встроен в JDK; анализ в JMC |
| **JMC / VisualVM** | GUI‑анализ (JFR/heap и т.п.) | — | локал/офлайн | удобная визуализация |
| **YourKit / JProfiler** | глубокая интерактивная отладка | средняя | стейдж/локал | коммерческие, удобные в IDE |

---

## ⚙️ async-profiler (рекомендую как «первый номер»)

**Запуски (самое нужное):**
```bash
# 30 сек CPU-профиль
./profiler.sh -d 30 -e cpu   -f cpu.svg   <pid>

# 30 сек реального времени (учитывает ожидания/IO/блокировки)
./profiler.sh -d 30 -e wall  -f wall.svg  <pid>

# 30 сек аллокаций (кто создаёт больше всего объектов)
./profiler.sh -d 30 -e alloc -f alloc.svg <pid>

# 30 сек блокировок (lock contention)
./profiler.sh -d 30 -e lock  -f lock.svg  <pid>
```

Открой `*.svg` в браузере — это **flame graph**: ширина «пламени» = доля времени/CPU.  
Снизу вверх — стек вызовов, слева‑направо — «шире = горячее».

**Шпаргалка по чтению flame graph:**
- Ищи **широкие пластины** вверху (конечные методы) и в середине (общие пути).
- Сравни `cpu.svg` и `wall.svg`: если `wall` шире — значит ждёшь (IO/локи).
- В `alloc.svg` — классы, которые «мусорят» и давят на GC.

> Примечания: на Linux нужен `perf_event_paranoid`/`kernel.kptr_restrict` в адекватных значениях (обычно в проде уже настроено).

---

## ⚙️ JFR (Java Flight Recorder)

**Старт/стоп через `jcmd`:**
```bash
# Профиль на 2 минуты
jcmd <pid> JFR.start name=profile settings=profile filename=/tmp/run.jfr duration=2m
# (опционально) принудительный дамп в файл
jcmd <pid> JFR.dump  name=profile filename=/tmp/run.jfr
# остановка
jcmd <pid> JFR.stop  name=profile
```

**Анализ:** открой `/tmp/run.jfr` в **JDK Mission Control (JMC)**.  
Там есть виды: CPU, Allocation, Locks, File/Socket IO, GC, Exceptions, Safepoints, Latency Analysis, Thread overview.

**Советы:**
- Для быстрой диагностики используй пресет `settings=profile` (агрессивней, но безопасно).  
- На проде длительность 1–5 минут обычно достаточно; включай выборочно, чтобы не шуметь логами/диском.

---

## 🔎 Что смотреть (чеклист)

1. **CPU (async `cpu.svg` или JFR CPU):** широкие пластины → ваши методы/циклы/парсинг/сериализация?  
2. **Wall time (`wall.svg`):** если шире, чем CPU, значит **ждёшь** — смотри верх стека: `Future.get`, `Selector.select`, `Object.wait`, JDBC/HTTP вызовы.  
3. **Allocations (`alloc.svg` / JFR Allocation):** кто выделяет чаще всего → уменьшить промежуточные объекты, пулы, кеши, стримы/боксинг.  
4. **Locks (`lock.svg` / JFR Locks):** узкие места синхронизации — `synchronized`, `ReentrantLock`, `ConcurrentHashMap.compute*`.  
5. **GC/JFR:** частые паузы, high allocation rate, promotion failures → тюнинг или уменьшение аллокаций.  
6. **Safepoints/JFR:** долгие `Stop-The-World` → проверь компиляцию, deopt, Code Cache, большие структуры.

---

## 🧪 Практика запуска (быстрые рецепты)

**Локально под нагрузкой:**
```bash
./profiler.sh -d 60 -e cpu -f cpu.svg <pid>
open cpu.svg   # macOS
```

**На стейдже/проде (JFR):**
```bash
jcmd <pid> JFR.start name=prod settings=profile filename=/tmp/prod.jfr duration=3m
# после — забрать файл и открыть в JMC
```

**Kubernetes (пример):**
```bash
kubectl exec -it <pod> -- jcmd 1 JFR.start name=pod settings=profile filename=/tmp/pod.jfr duration=2m
kubectl cp <ns>/<pod>:/tmp/pod.jfr ./pod.jfr
```

---

## ⚠️ Безопасность и влияние

- **async-profiler** и **JFR** считаются низконакладными и безопасны для кратковременного запуска на проде.  
- Избегай «инструментаторов» с интрузивной bytecode-инструментацией на проде (если не уверен в их накладных расходах).  
- Проверяй доступность прав для профилирования (perf events / attach / SecurityManager off).

---

## 🧭 Выбор стратегии (что запускать сначала)

1) Непонятно, где CPU → `async-profiler cpu` (30–60 сек).  
2) Подвисания/таймауты → `async-profiler wall` **или** JFR.  
3) Память/мусор → `async-profiler alloc` **и/или** JFR Allocation + потом heap dump при необходимости.  
4) Блокировки → `async-profiler lock` **или** JFR Locks.  

> Часто хватает 2–3 коротких сессий по 1–3 минуты, чтобы получить ясную картину.

---

## 🧾 Мини-FAQ

- **Flame graph «прыгает» между запусками?** Нормально — берите **несколько** сессий и ищите повторяющиеся широкие «пластины».  
- **Почему `wall` сильно шире `cpu`?** Вы тратите время в ожидании (сеть/диск/замки). Ищите верхние кадры.  
- **Можно ли включить JFR при старте?** Да: `-XX:StartFlightRecording=filename=/tmp/app.jfr,settings=profile`.  
- **Нет доступа к perf (Linux)?** Попроси DevOps снизить `kernel.perf_event_paranoid`, или используй JFR.

---

## Итог
Профилирование — самый быстрый способ понять, *где именно* утекает время/CPU/память.  
Начинай с `async-profiler` (CPU/Wall/Alloc/Lock), для событий JVM и офлайн‑анализа — **JFR + JMC**. Сохраняй артефакты (`*.svg`, `*.jfr`) — их удобно разбирать повторно.







### async-profiler (низкая нагрузка)
```bash
./profiler.sh -d 30 -e cpu  -f cpu.svg   <pid>   # CPU
./profiler.sh -d 30 -e wall -f wall.svg  <pid>   # стенка времени (видно блокировки/IO)
./profiler.sh -d 30 -e alloc -f alloc.svg <pid>  # аллокации
./profiler.sh -d 30 -e lock  -f lock.svg  <pid>  # блокировки
```

### JFR (Java Flight Recorder)
```bash
jcmd <pid> JFR.start name=prod settings=profile filename=/tmp/run.jfr duration=5m
jcmd <pid> JFR.dump  name=prod filename=/tmp/run.jfr
jcmd <pid> JFR.stop  name=prod
```
Анализ через JMC: события GC, CPU, file/net IO, аллокации, latency.

---








## 7) Monitoring (Grafana и др.)

# Monitoring в JVM (п.7)

---

## 🧠 Что такое мониторинг

**Monitoring** — это постоянное наблюдение за JVM и приложением во времени:  
сбор метрик (CPU, память, GC, потоки, latency, throughput) и визуализация в дашбордах.

> 🔹 Профилирование отвечает *почему сейчас медленно*,  
> 🔹 Мониторинг — *что пошло не так и когда именно*.

---

## 🧩 Основные цели

- Следить за **памятью** (Heap, Metaspace, Off-Heap);
- Контролировать **GC-паузы** и частоту сборок;
- Наблюдать **количество потоков**, очередей, активных запросов;
- Держать под контролем **CPU**, **IO**, **network**, **latency**;
- Вовремя узнавать о **ошибках и утечках**.

---

## 🧰 Инструменты JVM-мониторинга

| Категория | Примеры | Когда применять |
|------------|----------|----------------|
| **JMX (Java Management Extensions)** | встроено в JVM, `jconsole`, `VisualVM`, `Prometheus JMX Exporter` | базовые метрики в любой Java-сервис |
| **Metrics API / Micrometer** | `Micrometer`, `Dropwizard Metrics`, `Micronaut metrics` | бизнес-метрики, интеграция с Prometheus/Grafana |
| **Prometheus + Grafana** | `jmx_exporter`, `otel-collector`, `Micrometer Registry` | стандарт для продакшн-дашбордов |
| **APM** | `New Relic`, `Datadog`, `AppDynamics`, `Elastic APM` | централизованный мониторинг и трассировка |
| **JFR Streaming** | `jcmd JFR.configure` + JDK Flight Recorder API | низкоуровневый мониторинг без агентов |

---

## ⚙️ JVM метрики, которые нужно знать

| Метрика | Что показывает | Как получить |
|----------|----------------|---------------|
| `java.lang:type=Memory` | Heap/Non-Heap, usage, peak | JMX, VisualVM |
| `java.lang:type=GarbageCollector` | время и частота GC | JMX, Prometheus |
| `java.lang:type=Threading` | активные потоки, daemon, deadlock | JMX, jconsole |
| `java.lang:type=OperatingSystem` | CPU load, file descriptors | JMX |
| `CodeCacheUsage` | заполнение кэша JIT | JMX, jcmd |
| `ClassLoading` | загрузка/выгрузка классов | JMX |

---

## 📊 Что мониторить в продакшене

| Категория | Метрика | Порог / симптом |
|------------|----------|----------------|
| **Память** | Heap usage > 80% | возможная утечка |
| | Metaspace растёт | частые classloader-создания |
| **GC** | Pause time > 200 ms | GC-pressure |
| | Full GC часто | нехватка heap |
| **CPU** | > 80% дольше 1 мин | горячие циклы, конкуренция |
| **Потоки** | > N базовой нормы | утечки, зависания |
| **Code Cache** | > 95% | JIT-компиляция остановится |
| **Latency / Errors** | p95 latency растёт, error rate > 0.5% | деградация |

---

## 🔍 Как подключить метрики

### Prometheus JMX Exporter
```bash
java -javaagent:jmx_prometheus_javaagent.jar=8080:config.yaml -jar app.jar
```
Пример `config.yaml`:
```yaml
rules:
  - pattern: 'java.lang<type=Memory><HeapMemoryUsage>(used|committed|max):'
    name: jvm_memory_$1_bytes
    type: GAUGE
```

### Micrometer + Spring Boot Actuator
```groovy
implementation 'io.micrometer:micrometer-registry-prometheus'
implementation 'org.springframework.boot:spring-boot-starter-actuator'
```
→ `/actuator/prometheus` отдаёт все JVM + бизнес-метрики.

---

## 📈 Как смотреть

- **Grafana** — визуализация и алерты (heap usage, GC time, latency).  
- **VisualVM / JConsole** — локальные метрики JVM.  
- **Mission Control (JMC)** — подключение к живой JVM (JMX/JFR streaming).  
- **APM-сервисы** — трассировка HTTP, БД, ошибок, транзакций.

---

## 🧾 Мини-чеклист Dev/Prod-мониторинга

1. В Dev: VisualVM + JConsole для быстрых проверок.  
2. В Stage/Prod: JMX Exporter + Prometheus + Grafana.  
3. Для аллокаций/GC — включи JFR streaming.  
4. Для бизнес-метрик — Micrometer + Actuator.  
5. Настрой алерты на heap, GC, CPU, error rate, latency.  
6. Периодически проверяй дашборды — «чтобы не узнать о проблеме из тикета».

---

## Итог

Мониторинг — твоя «система раннего предупреждения».  
Он показывает тренды, указывает на утечки и деградации задолго до аварии.  
Комбинируй JMX, Prometheus/Grafana и JFR, чтобы видеть как низкоуровневые, так и бизнес‑метрики.







**Основные места куда смотреть в графафне:**
- **Память:** Old/Young usage, allocation rate, частота/длительность пауз GC.
- **CPU:** user/sys, loadavg, «горячие» потоки; в контейнерах — **CPU throttling** (cgroup `throttled_time`).
- **GC time %** (mutator utilization), promotion failures/Full GC.
- **Потоки/дескрипторы:** JMX/`jcmd VM.system_properties`, NMT.
- **Диск/сеть:** iowait, saturation.

**CLI-хелп:**
```bash
top | pidstat -t 1 | vmstat 1 | iostat -xz 1
jstat -gcutil <pid> 1s
jcmd  <pid> VM.native_memory summary
```

**Контейнеры:** проверь лимиты (`-XX:MaxRAMPercentage`, CPU limits), `OOMKilled`, `CrashLoopBackOff`.

---












## 8) Анализ аварийных дампов (Паньгин, ч.1–2)

Фокус на чтении `hs_err_pid`:
- VM Args, версия JDK/OS, карта памяти.
- Стек упавшего потока, *Problematic frame*.
- Список загруженных JNI-библиотек.
- Формируем гипотезу: где упало (JNI/driver/JIT), какой шаг изоляции/воспроизведения.

---

## 9) Разбор сложных OOM

**Основные виды и куда смотреть:**
- `Java heap space` → heap dump, удерживающие ссылки.
- `GC overhead limit exceeded` → слишком частый GC (обычно из-за утечек/высоких аллокаций).
- `Metaspace` → рост классов/класс-лоадеров (`-XX:MaxMetaspaceSize`, анализ ClassLoader Leak).
- `Direct buffer memory` → Netty/NIO; контролируй `-XX:MaxDirectMemorySize`, ищи утечки `ByteBuffer`.
- `Unable to create new native thread` → лимит ОС/контейнера по нитям (ulimit/pids), тред-лик.
- **Native memory leak** (вне кучи): включи NMT и смотри отчёт.
```bash
-XX:NativeMemoryTracking=summary   # или detail
jcmd <pid> VM.native_memory summary
```

---

## 10) Ключи JVM для автосохранения при краше

```bash
-XX:+HeapDumpOnOutOfMemoryError
-XX:HeapDumpPath=/dumps
-XX:+CreateCoredumpOnCrash
-XX:ErrorFile=/logs/hs_err_pid%p.log

# план Б (может не сработать при тяжёлом OOM):
-XX:OnError="jcmd %p Thread.print > /logs/threaddump.%p.txt"
-XX:OnOutOfMemoryError="jcmd %p GC.heap_info"
# опционально и осторожно:
-XX:+CrashOnOutOfMemoryError

# выключает оптимизацию HotSpot, при которой у часто бросаемых исключений (например, NullPointerException, ArrayIndexOutOfBoundsException, ArithmeticException, ClassCastException и др.) перестают заполнять стек после «прогрева» кода.
-XX:-OmitStackTraceInFastThrow
```

---

## 11) Анализ логов GC


# Garbage Collection (GC) в JVM (п.8)

---

## 🧠 Что такое GC (Garbage Collection)

Когда ты создаёшь объекты в Java (`new Something()`), они попадают в **кучу (Heap)**.  
Когда на них больше **нет ссылок**, они становятся «мусором» (garbage).  
**Сборщик мусора (Garbage Collector)** — это часть JVM, которая **освобождает память**, удаляя ненужные объекты.

---

## 🧩 Как это устроено по-простому

JVM делит кучу на «поколения» (generations):

| Область | Назначение | Что в ней |
|----------|-------------|------------|
| **Young Generation** | новые объекты | создаются и быстро умирают |
| **Old (Tenured) Generation** | «долгоживущие» объекты | те, что пережили несколько сборок |
| **Metaspace** | метаданные классов | не входит в heap |

### Жизненный цикл объекта
1. Объект создаётся в **Eden** (часть Young Gen).  
2. Если переживает несколько «Minor GC» — перемещается в Old Gen.  
3. Когда Old Gen заполняется — запускается **Major (Full) GC**, который проходит по всей куче.

---

## 🚀 Типы GC-сборщиков (на выбор JVM)

| Сборщик | Где используется | Особенности |
|----------|------------------|--------------|
| **Serial GC** | маленькие приложения | простой, однопоточный |
| **Parallel GC** | серверные нагрузки | быстрый, но стоп-паузы |
| **G1 GC (Garbage-First)** | по умолчанию в JDK 11+ | баланс между скоростью и паузами |
| **ZGC / Shenandoah** | большие кучи, low-latency системы | минимальные паузы (<10 мс) |

---

## 🧩 Что важно смотреть

1. **Время пауз (Pause Time)** — сколько JVM «замирала» (Stop-the-World).  
2. **Частота сборок** — как часто идут Minor/Full GC.  
3. **Использование Heap** — растёт ли Old Gen → возможная утечка.  
4. **Promotion failures / Full GC spikes** → heap мал или объекты живут слишком долго.

---

## ⚙️ Как включить логи GC

### JDK 9+
```bash
-Xlog:gc*,gc+heap=debug:file=gc.log:time,uptime,level,tags
```

### JDK 8
```bash
-XX:+PrintGCDetails -XX:+PrintGCDateStamps -Xloggc:gc.log
```

Пример строки:
```
[0.123s][info][gc] GC(0) Pause (G1 Evacuation Pause) 100M->20M(512M) 4.56ms
```
📖 До GC было 100МБ, после — 20МБ, пауза 4.56 мс.

---

## 🔍 Анализ и визуализация

- **VisualVM** → вкладка *Monitor → GC* — график пауз и heap.  
- **Java Mission Control (JMC)** → JFR → GC events.  
- **Grafana / Prometheus** → метрики `jvm_gc_pause_seconds`, `jvm_memory_used_bytes`.

---

## 🧰 Полезные флаги для настройки

```bash
-XX:+UseG1GC                            # включить G1GC
-XX:MaxGCPauseMillis=200                # цель по паузам
-XX:InitiatingHeapOccupancyPercent=45   # когда начинать сборку
-XX:ConcGCThreads=4                     # потоки фоново собирающего GC
```

Для экспериментов:
```bash
-XX:+UseZGC
-XX:+UseShenandoahGC
```

---

## 🚨 Признаки проблем с GC

| Симптом | Возможная причина |
|----------|------------------|
| Частые Full GC | утечка / маленький heap |
| Высокий CPU у GC | слишком много короткоживущих объектов |
| Долгие паузы > 1с | неподходящий тип GC |
| Heap не освобождается | утечка, нужна heap dump диагностика |

---

## 🧾 Краткий чеклист

1. Включи GC-логи на тесте.  
2. Следи, чтобы Heap usage снижался после сборки.  
3. Смотри Pause Time — желательно <200 мс.  
4. Контролируй рост Old Gen.  
5. Настрой GC под свои цели (latency / throughput).

---

## 💡 Итог

Garbage Collector — это «дворник» JVM, убирающий неиспользуемые объекты.  
Если он не справляется — включи GC-логи, проверь heap, сделай heap dump и подбери нужный тип GC.  
Главное — наблюдать за паузами и не мешать сборщику работать 😄








**Включение:**
```bash
# JDK 9+
-Xlog:gc*,safepoint:file=/logs/gc.log:uptime,level,tags
# JDK 8
-XX:+PrintGCDetails -XX:+PrintGCDateStamps -Xloggc:/logs/gc.log -XX:+PrintTenuringDistribution
```

**На что смотреть:** алгоритм (Parallel/CMS/G1/ZGC/Shenandoah), частота/длительность пауз, причины (Allocation Failure, Metadata GC Threshold, Humongous), доли фаз (remark/cleanup), promotion rate, Survivor/Tenuring.

**Инструменты:** GCViewer, gceasy, JMC (JFR GC view).

**Типичные проблемы/подходы:**
- Слишком высокий **allocation rate** → уменьшить аллокации (пулы/реюз).
- Частые Young GC + промоушены → увеличить young/heap, тюнинг G1 (`MaxGCPauseMillis`, `IHOP`).
- Full GC / Promotion failed → нехватка памяти/фрагментация.
- G1 Humongous → избегать огромных объектов, тюнинг `G1HeapRegionSize`.

---

## 12) Быстрый чек-лист при инциденте

1. Забрать 3–5 **thread dump**, **JFR** на 2–5 мин, **GC-логи**, `hs_err_pid` (если есть), системные метрики, версию JDK и полный набор флагов:
   ```bash
   jcmd <pid> VM.flags
   jcmd <pid> VM.system_properties
   ```
2. Если «память»: **heap dump** (+ NMT для native).
3. Если «высокий CPU»: async-profiler (`cpu`/`wall`) + hot threads.
4. В контейнерах: проверить **лимиты**, **throttling**, статусы Pod (`OOMKilled`, `CrashLoopBackOff`).

---


-Xmx<size> — верхний предел heap’а (Java-кучи).
-Xss<size> — размер стека на каждый поток JVM.

тормозит java
1. gc или не gc
  включаем gc log
  смотрим в gc viewer'е
  смотрим на графики
2. но не все паузы из-за gc, тогда проверяем насколько часто у нас safepoint'ы
  опция показывает сколько java была на паузе:
  -XX:+PrintGCApplicationStoppedTime 
  в java-11 опция выглядит так:
  -Xlog:safepoint*=info
3. например, может еще тормозить, потому что в code cache кончилось место ...
  тогда в stdout напечатается строка "Complier has been disabled."
  этой опцией можно увеличить место под code cache
  -XX:ReservedCodeCacheSize=
